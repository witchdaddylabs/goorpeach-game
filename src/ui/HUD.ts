/**
 * HUD — lives (hearts), ammo count, and level timer. Uses JetBrains Mono for
 * tabular elements and Bungee for numbers. Colours from config.ts tokens only;
 * text/background pairings must meet WCAG 2.1 contrast minimums.
 *
 * TODO: implement. See CLAUDE.md working pattern, step 10.
 */
export class HUD {
  // TODO: draw hearts, ammo, timer; update on game events.
}
